name: gcms
# These are the minimum number of packages that need to be specified
#     Of course the dependencies of the specified packages need to be installed
#         With the below version numbers speficied everything should be compatible for the software to work

# these do not have version numbers specified 
#   Solving the environment is different on different operating systmes
#   Right now bokeh is pretty old and that is causing other old packages to be installed
dependencies:
- python
- numpy
- pandas
- tk
- bokeh=2.3

#- pip: these things need to be installed manually with pip in the gcms environment
#  - peakutils
#  - netCDF4==1.6 # need to install HDF5 with brew in order to install netCDF4? See environment_notes.txt
